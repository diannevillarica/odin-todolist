import setAttributes from "./helpers/setAttributes";

const handleProjectFormSubmit = (event) => {
  event.preventDefault();
  // console.log("this is working");

  console.log(event);

  const addToProjectList = (projectName) => {};
  // get input value and add to project list
};

export default handleProjectFormSubmit;
